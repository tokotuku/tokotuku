{
  "name": "@tokotuku-starter/web",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "astro": "astro",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "db:seed:local": "wrangler d1 execute DB --local --file=./seed/seed.sql",
    "db:seed:remote": "wrangler d1 execute DB --remote --file=./seed/seed.sql",
    "r2:seed:local": "wrangler r2 object put tokotuku-starter-images/products/widget.svg --local --file=./seed/images/widget.svg --content-type=image/svg+xml && wrangler r2 object put tokotuku-starter-images/products/gadget.svg --local --file=./seed/images/gadget.svg --content-type=image/svg+xml && wrangler r2 object put tokotuku-starter-images/products/gizmo.svg --local --file=./seed/images/gizmo.svg --content-type=image/svg+xml",
    "lint": "biome check .",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.3",
    "@tokotuku-starter/ui": "workspace:*",
    "@tokotuku/theme": "latest",
    "astro": "^7.0.9",
    "better-auth": "^1.6.23"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "wrangler": "^4.110.0"
  }
}
