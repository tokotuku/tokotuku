
# dependencies
node_modules
.pnpm-store/

# build output
dist
.astro

# cloudflare / wrangler local dev (belt-and-suspenders: each client app also
# ships this in its own .gitignore via create-karsa, but a repo-root rule
# means it's excluded even for a path that temporarily has no app of its own,
# e.g. mid-restructure)
.wrangler/
.dev.vars
.dev.vars.*
worker-configuration.d.ts

# testing
coverage
playwright-report
test-results
blob-report

# env
.env
.env.*
!.env.example

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# moon
.moon/cache
.moon/docker

# verdaccio local auth token
.npmrc

# graphify skill output/cache
graphify-out
