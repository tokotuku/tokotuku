---
"@karsa/auth": minor
"@karsa/core": minor
"@karsa/jarene": minor
"create-karsa": minor
---

Redesign the shared authentication shell around a responsive editorial image panel, add the
`authPanelWidgets` module extension point, and ship `@karsa/jarene` with server-rendered,
source-attributed quotes for the auth panel. Fresh scaffolds include Jarene by default and can
remove it with `karsa remove jarene`.
