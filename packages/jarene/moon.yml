$schema: "https://moonrepo.dev/schemas/project.json"
language: "typescript"
stack: "frontend"
layer: "library"

tasks:
  build:
    command: "tsup"
    deps: ["core:build"]
    inputs:
      - "src/**/*"
      - "tsup.config.ts"
      - "tsconfig.json"
    outputs:
      - "dist"

  lint:
    command: "biome check ."
    inputs:
      - "src/**/*"
      - "*.json"

  typecheck:
    command: "astro check"
    deps: ["core:build"]
    inputs:
      - "src/**/*"
      - "tsconfig.json"

  test:
    command: "vitest run"
    inputs:
      - "src/**/*"
      - "vitest.config.ts"
