{
  "name": "create-karsa",
  "version": "0.3.0",
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "type": "module",
  "bin": {
    "create-karsa": "./dist/bin.js"
  },
  "files": [
    "dist",
    "src/template"
  ],
  "devDependencies": {
    "@karsa/config": "^0.3.0",
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0"
  }
}
