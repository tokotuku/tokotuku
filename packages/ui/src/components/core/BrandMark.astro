---
import defaultMark from "../../assets/brand/karsa/karsa-mark.png";

interface Props {
  alt?: string;
  class?: string;
  size?: number;
}

const { alt = "", class: className, size = 40 } = Astro.props;

import "./BrandMark.css";
---

<img
  class:list={["karsa-brand-mark", className]}
  src={defaultMark.src}
  width={size}
  height={size}
  alt={alt}
  loading="eager"
  decoding="async"
  style={`width:${size}px;height:${size}px`}
/>
