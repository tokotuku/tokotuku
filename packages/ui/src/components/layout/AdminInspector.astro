---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<section class="inspector-section">
  <h2>{title}</h2>
  {description && <p>{description}</p>}
  <slot />
</section>
