$schema: "https://moonrepo.dev/schemas/project.json"
language: "typescript"
stack: "frontend"
layer: "library"

tasks:
  lint:
    command: "biome check ."
    inputs:
      - "src/**/*"
      - "*.json"

  typecheck:
    command: "astro check"
    inputs:
      - "src/**/*"
      - "tsconfig.json"

  test:
    command: "vitest run src/theme.test.ts"
    inputs:
      - "src/theme.ts"
      - "src/theme.test.ts"
