$schema: "https://moonrepo.dev/schemas/project.json"
language: "typescript"
stack: "frontend"
layer: "library"

dependsOn:
  - "tokens"

tasks:
  build:
    command: "tsup"
    deps:
      - "tokens:build"
    inputs:
      - "src/**/*"
      - "tsup.config.ts"
      - "tsconfig.json"
    outputs:
      - "dist"

  typecheck:
    command: "tsc --noEmit -p tsconfig.json"
    deps:
      - "tokens:build"
    inputs:
      - "src/**/*"
      - "tsconfig.json"

  lint:
    command: "biome check ."
    inputs:
      - "src/**/*"
      - "*.ts"
      - "*.json"

  test:
    command: "vitest run"
    deps:
      - "tokens:build"
    inputs:
      - "src/**/*"
      - "vitest.config.ts"
