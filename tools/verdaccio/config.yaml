storage: /verdaccio/storage

auth:
  htpasswd:
    file: /verdaccio/storage/htpasswd
    # Self-registration is closed -- add team accounts with `npm adduser`
    # (or edit the htpasswd file directly) BEFORE this takes effect, since
    # once it's live `npm adduser` is rejected for everyone. See README.md.
    max_users: -1

uplinks:
  npmjs:
    url: https://registry.npmjs.org/

packages:
  # Private -- catalog/orders are commercial modules, never published to
  # public npm. Specific patterns must come before the @takontuku/* one
  # below: Verdaccio uses the first pattern that matches.
  "@takontuku/catalog":
    access: $authenticated
    publish: $authenticated
    unpublish: $authenticated

  "@takontuku/orders":
    access: $authenticated
    publish: $authenticated
    unpublish: $authenticated

  # Public -- core/ui/auth/config/create-takontuku live on real npm once
  # published there; this registry only needs to serve them locally during
  # development (moon run verdaccio:up, tools/e2e-install/). No npmjs proxy
  # for this scope, on purpose: every e2e gate publishes a fresh version and
  # resolves it by "latest", and a proxy would let that resolve against the
  # real npm registry's @takontuku/* packages instead once they exist there.
  "@takontuku/*":
    access: $all
    publish: $authenticated
    unpublish: $authenticated

  "**":
    access: $all
    publish: $authenticated
    unpublish: $authenticated
    proxy: npmjs

middlewares:
  audit:
    enabled: true

log:
  type: stdout
  format: pretty
  level: http
