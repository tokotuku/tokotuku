# playground

A minimal Vite app for manually exercising Tokotuku UI components during development — not published, not part of the docs site.

## Development

```sh
bun run dev
```

## Status

Currently just proves the workspace wiring (importing `@tokotuku/theme` and adopting its stylesheet) works end to end. Import real components from `@tokotuku/elements` here as they're built.
