---
title: AppTopbar
description: Workspace title, theme selector, and action area.
sidebar:
  order: 3
---

`AppTopbar` renders a compact workspace heading, a light/dark toggle, and a default slot for actions.

```astro
---
import Button from "@takontuku/ui/Button.astro";
import AppTopbar from "@takontuku/ui/AppTopbar.astro";
---

<AppTopbar title="Commerce workspace">
  <Button variant="secondary" size="sm">Sign out</Button>
</AppTopbar>
```

## Props and slots

| Name | Kind | Default | Purpose |
| --- | --- | --- | --- |
| `title` | Prop (`string`) | `"Workspace"` | Workspace label |
| default | Slot | — | Actions aligned to the right |

## Theme behavior

The toggle writes light or dark to document.documentElement.dataset.theme and persists the choice under takontuku-theme in local storage. OS accessibility modes remain available.
