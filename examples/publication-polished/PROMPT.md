# Luna phase prompt

Generator pin: `gpt-5.6-luna`, reasoning `max`.

Inspect → foundation → content → design/assets. Build Karsa Journal with the content module's
published, draft, archived, preview, RSS, and sitemap flows intact. Use a focused palette and one
deterministic local hero asset with a manifest; do not fork module-owned routes or use external
imagery. Run desktop/mobile smoke when available and all checks, stopping on failure.
