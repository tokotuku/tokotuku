# Luna phase prompts

Generator pin: `gpt-5.6-luna`, reasoning `max`.

Inspect → foundation → content → design/assets. Build Arunika Energi profile, then define palette and local asset manifest before UI changes. Generate one local asset per call; no stock URLs/placeholders/watermarks. Run desktop/mobile smoke and all checks.
