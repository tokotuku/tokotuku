# Luna phase prompts

Generator pin: `gpt-5.6-luna`, reasoning `medium`.

Inspect → foundation only. Install `booking` through the Takontuku CLI (bringing `orders` and `catalog`), apply migrations, and keep Teman Ekor default: no services, seed, theme override, or assets. Run all checks and stop on failure.
