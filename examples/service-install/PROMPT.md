# Luna phase prompts

Generator pin: `gpt-5.6-luna`, reasoning `max`.

Inspect → foundation only. Install `booking` through the Karsa CLI (bringing `orders` and `catalog`), apply migrations, and keep Teman Ekor default: no services, seed, theme override, or assets. Run all checks and stop on failure.
