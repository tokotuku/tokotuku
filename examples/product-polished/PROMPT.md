# Luna phase prompts

Generator pin: `gpt-5.6-luna`, reasoning `max`.

Inspect → foundation → content → design/assets. Complete Racik Rasa catalog, then create a palette and local manifest; generate a hero plus at least four local product images, one asset per call. No stock URLs/placeholders/watermarks. Verify responsive commerce flows and all checks.
