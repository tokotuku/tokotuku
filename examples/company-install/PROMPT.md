# Luna phase prompts

Generator pin: `gpt-5.6-luna`, reasoning `max`.

Inspect → foundation only. Keep the scaffold baseline: install no commerce modules, add no custom content, theme override, seed, or assets. Run `cf-typegen`, migration sync/apply, lint, typecheck, build, and HTTP smoke checks. Stop on failure; at most two bounded repairs.
