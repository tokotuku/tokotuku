# Luna phase prompts

Generator pin: `gpt-5.6-luna`, reasoning `medium`.

Inspect → foundation only. Install `orders` through the Takontuku CLI (which brings `catalog`), apply migrations, and keep Racik Rasa otherwise default: no products, seed, theme override, or assets. Run all checks and stop on failure.
