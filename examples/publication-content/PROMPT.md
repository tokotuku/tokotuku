# Luna phase prompt

Generator pin: `gpt-5.6-luna`, reasoning `max`.

Inspect → foundation → content. Build Karsa Journal publication copy with idempotent local posts
covering published, draft, and archived states. Preserve authenticated draft preview, publishing,
RSS, sitemap, and archive behavior owned by `@karsa/content`; do not add a theme override or
generated imagery. Run all checks and stop on failure.
