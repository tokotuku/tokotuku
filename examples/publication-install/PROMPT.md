# Luna phase prompt

Generator pin: `gpt-5.6-luna`, reasoning `max`.

Inspect the Karsa scaffold, install the closed-auth content preset, sync/apply migrations, and
stop before adding posts, theme overrides, or custom assets. Run all install checks and stop on
failure.
