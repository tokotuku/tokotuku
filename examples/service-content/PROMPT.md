# Luna phase prompts

Generator pin: `gpt-5.6-luna`, reasoning `max`.

Inspect → foundation → content. Seed three Teman Ekor services, including schedule modes `range` and `slot`; seed must be local-only and idempotent. Do not add theme overrides or imagery. Verify booking flow and admin booking for both modes plus all checks.
